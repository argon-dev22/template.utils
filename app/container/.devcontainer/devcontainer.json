{
  "name": "container", // devcontainer name
  "service": "container", // service name in docker-compose.yml
  "workspaceFolder": "/app", // WORKDIR in Dockerfile
  "dockerComposeFile": "../../compose.yml", // docker-compose.yml file path
  "customizations": {
    "vscode": {
      "extensions": [
        // Add extensions here
      ]
    }
  }
}
